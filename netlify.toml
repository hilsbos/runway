[build]
  command = "npm run build"
  publish = "dist"

# SPA fallback: serve index.html for any path (state lives in the query string)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
